package Interface2;

//interface cannot have body
//interface contains the abstract method only,& there is no need to write "abstract" keyword.
//We cant create the objects of interface because it contains abstract methods.

public interface Demo {
    public int a = 10;
    public void print();
}
